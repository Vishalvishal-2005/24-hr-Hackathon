package com.example.admin;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class AdminApplicationTests {

	@Test
	/**
	 * This method is used to verify that the application context loads successfully.
	 * It is typically used in unit tests to ensure that the Spring application context
	 * is properly initialized and that all necessary beans are available.
	 *
	 * @throws Exception if the application context fails to load due to configuration issues
	 * or missing dependencies.
	 */
	void contextLoads() {
	}

}
